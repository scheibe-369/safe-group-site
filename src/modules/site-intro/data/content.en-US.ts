import type { SiteIntroContent } from "../types";

/** Copy pt-PT da cortina de entrada. */
export const introContentEnUS: SiteIntroContent = {
  tag: "//loading safe group...",
  steps: ["00", "23", "65", "100"],
};
