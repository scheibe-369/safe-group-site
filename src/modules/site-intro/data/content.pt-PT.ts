import type { SiteIntroContent } from "../types";

/** Copy pt-PT da cortina de entrada, a lingua de origem. */
export const introContentPtPT: SiteIntroContent = {
  tag: "//a carregar safe group...",
  steps: ["00", "23", "65", "100"],
};
