import type { SiteIntroContent } from "../types";

/** Copy pt-PT da cortina de entrada. */
export const introContentEs: SiteIntroContent = {
  tag: "//cargando safe group...",
  steps: ["00", "23", "65", "100"],
};
