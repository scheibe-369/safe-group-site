export type LanguageSwitcherContent = {
  /** Titulo da coluna, no registo dos outros titulos do menu. */
  heading: string;
  /** Prefixo do rotulo acessivel de cada opcao, seguido do nome do idioma. */
  switchTo: string;
};
