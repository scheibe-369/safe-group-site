/**
 * Direção tipográfica ativa. Manrope é a direção aprovada e o valor por omissão.
 * Reescrito por `scripts/set-typography.mjs` para os builds de comparação.
 *
 * Só a variante escolhida entra no grafo de módulos, por isso cada build
 * carrega apenas as duas fontes dessa direção.
 */
export { variant } from "./variants/manrope";
